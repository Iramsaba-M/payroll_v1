
import ErrorScreen from '../../../../errorhandling/ErrorScreen'

const LoanApprovalSettings = () => {
  const errorCode=404
  const errorCode=404
  return (
    <div>
      <ErrorScreen errorCode={errorCode} />
      <ErrorScreen errorCode={errorCode} />
    </div>
  )
}

export default LoanApprovalSettings
