// config.js
const PayrollnotificationConfig = {
  buttons: [
    { label: "Roll Out", action: () => console.log("Roll Out") },
    { label: "Hold", action: () => console.log("Hold") }
  ]
};

export default PayrollnotificationConfig;
